/**
 * HTML export theme — colors used in markdown HTML export.
 * These match the app's visual design for exported documents.
 */
export const EXPORT_THEME = {
  bodyText: '#1a1a1a',
  codeBackground: '#f5f5f5',
  blockquoteBorder: '#ddd',
  blockquoteText: '#666',
  maxWidth: '800px',
} as const;
